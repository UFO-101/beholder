{
  "name": "beauty-heatmap-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for London Beauty Heatmap",
  "type": "module",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}